<div class="section no-pad-bot" id="index-banner">
  <div class="container">
    <h1 class="header center">{{ l.title }}</h1>
    <h2 class="header center black-text">{{ l.title }}</h2>
    <div class="row center">
      <img class="responsive-img" src="{{ cdn }}/img/logo.png" alt="{{ l.title }}">
      <h3 class="header col s12 light">{{ l.intro }}</h3>
    </div>
  </div>
</div>
